# Signal Harbor

Signal Harbor is the pending Strict benchmark case for a dense incident-monitoring SaaS dashboard. Its required journey is:

`monitoring -> incident-selected -> assigning -> assigned -> resolving -> resolved`

The recovery contract also requires a blocked assignment, a failed resolution, and retry paths. Keyboard navigation, deterministic visible focus, polite live-region announcements, contrast verification, and reduced-motion behavior remain requirements, not completed evidence.

The mouse participates as an `incident-dispatcher`. Default, resting, and recovery appearances retain the chest V/U as the sole clasped hand pair. Action mode is permitted only during `assigning` and `resolving`; it replaces the chest V/U with exactly one coherent action-hand pair. Other states must not imply action anatomy.

## Current truth

Only the case brief, mode declaration, derivation notes, prompt, and pending QA record exist at this stage. No source, output, generated raster asset, screenshot, comparison image, browser run, accessibility check, or acceptance evidence has been created. Every QA gate is therefore false and every applicable status is pending.

The public benchmark trigger upgrades the requested Standard mode to Strict. Future implementation must validate the workflow, ground all visual work in the required authorities, exercise the real browser journey at 1440×900, and record evidence before any gate changes.

## Future asset record

Image-generation prompts, inspected authorities, approved asset paths, and repair notes are intentionally absent while the case is pending. They must be added only after the required visual-grounding and image-generation work actually occurs.
