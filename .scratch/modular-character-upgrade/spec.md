# Modular character and interaction upgrade
Status: paused
Label: wontfix

## Current scope decision

The owner initially authorized modular face/body and UI improvements, then explicitly paused face processing and requested UI/motion improvement with the core character unchanged. The character portion is deferred by the owner, not completed; do not resume extraction, face cropping/replacement or body generation without a new request. The label means no further character work is to be actioned under the present scope.

## Answer

The prior compatible v2 metadata contract remains in place. Rejected generated bodies and common-mask face previews remain withdrawn. The latest approved complete-source characters are retained byte for byte in the local UI example.

The UI-only work is implemented and delivered: clearer controls, filters, two motion styles, stable async selection, cancellation, modal focus, saved settings/download, failure recovery and responsive layouts. Reusable plugin rules and installed personal entry were updated. See docs/upgrade-2026-09-10.md for current browser, package and installation evidence. Split-layer artwork acceptance is paused and is not a requirement for completing this UI-only request.
