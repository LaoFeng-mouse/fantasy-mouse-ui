# Local UI and motion upgrade — 2026-09-10

The owner's latest scope is UI and motion improvement with the core character frozen. Face processing, face replacement and body drawing are explicitly paused. Earlier rejected generated bodies and common-mask composites remain withdrawn; do not resume them as a prerequisite to UI work.

## Delivered implementation

The reusable Skill now has an explicit UI-only boundary. The workflow reference describes preservation of asset bytes and geometry, review of the existing flow, learning behavior principles from available design skills and primary documentation, restrained interaction timing, separate layout/animation layers, request revision guards, resource readiness and retry, dialogs, truthful export notifications, storage recovery, reduced motion and lifecycle cleanup. QA now checks actual intermediate animation states, interruption, keyboard containment, recovery and unchanged character assets. These are reusable rules, not a mandatory visual theme or bundled renderer.

The projectless live preview retains all six complete original JPGs and the byte-identical source viewport manifests. It adds sport/life filters, clearer hierarchy, focus and selection, two motion choices, image-ready switching, cancellation, large-image/help dialogs, local selection settings, JSON download, recoverable errors and responsive layouts. Native CSS/Web Animations/dialog and locally bundled Lucide icons are used; no online runtime dependency or user artwork is added to the plugin ZIP.

## Current evidence

- Edge/Playwright: six originals and source manifests preserved; selection/filtering; rapid replacement; visibly changing opacity and transform; completion/cancellation; local and system reduced motion; modal Tab/Shift+Tab/Escape and focus return; download content and export failure recovery; persistence and blocked storage; image failure/retry; file:// loading; pagehide handler cancellation. No page errors.
- Rendered desktop, tablet, mobile, dialog, save feedback and doubled-computed-text screenshots inspected. Viewports: 1440, 1024, 820, 620, 390 and 320px. Repaired modal focus escaping to browser chrome and enlarged-text header clipping. Recorded a 13.04-second real interaction video and same-context before/after comparison. Before images were rendered from the source backup taken at the start of this UI-only change.
- Checked contrast: text 13.4, muted 4.87, primary 6.21, focus 6.85, off switch 3.99 and radio 3.98. This is focused QA, not complete accessibility certification, measured FPS, actual browser 200% zoom or owner artwork acceptance.
- 232 tests passed, 1 existing Windows-inapplicable skip; typecheck, baseline four-asset verifier, official plugin/Skill validators and git diff --check passed. Two packages matched SHA-256 `5ADEFDB7EC5024C4154FC8E547850294D54171172815E9DB0085CBAF6A7EF849`, with exactly 24 files and no preview/support/user source files.

## Installation and handoff

`fantasy-mouse-ui@personal` is installed and enabled at `0.2.0+codex.20260910074027`. Its 24 installed files match working plugin source byte for byte. Installed baseline verification and mode resolution pass (Standard). The earlier duplicate `fantasy-mouse-ui@fantasy-mouse-ui` was removed with explicit authorization. Personal source was verified identical to the previous installed build before being updated.

Repository source: `plugins/fantasy-mouse-ui/`.
Personal source: `<user-profile>/plugins/fantasy-mouse-ui/`.
Installed cache: `<CODEX_HOME>/plugins/cache/personal/fantasy-mouse-ui/0.2.0+codex.20260910074027/`.

User-facing source, report, current screenshots/video and local ZIP are under the current task's outputs directory. Previous UI evidence is archived under work/ui-before; previous face experiments stay paused/withdrawn in work. The owner authorized synchronization of this source upgrade to the Git default branch and the personal plugin entry. This is separate from a new tagged Release or new-task automatic-loading verification; neither is claimed. Historical public v0.2.0 evidence remains historical and unchanged.
